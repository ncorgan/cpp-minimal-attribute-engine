# This file is auto-generated by tools/generate-travis-yaml.py.

before_install:
- if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo apt-get -y install libboost-dev; fi
- git submodule update --init --recursive
dist: trusty
language: generic
matrix:
  include:
  - compiler: gcc
    env: CC=gcc CXX=g++
    os: linux
  - compiler: clang
    env: CC=clang CXX=clang++
    os: linux
  - os: osx
  - addons:
      apt:
        packages:
        - g++-5
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-5 CXX=g++-5 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - g++-5
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-5 CXX=g++-5 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - g++-5
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-5 CXX=g++-5 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-6
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-6 CXX=g++-6 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - g++-6
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-6 CXX=g++-6 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - g++-6
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-6 CXX=g++-6 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-7
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-7 CXX=g++-7 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - g++-7
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-7 CXX=g++-7 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - g++-7
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-7 CXX=g++-7 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++2a
    os: linux
  - addons:
      apt:
        packages:
        - clang-4.0
        sources:
        - llvm-toolchain-trusty-4.0
    compiler: clang
    env: CC=clang-4.0 CXX=clang++-4.0 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - clang-4.0
        sources:
        - llvm-toolchain-trusty-4.0
    compiler: clang
    env: CC=clang-4.0 CXX=clang++-4.0 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - clang-4.0
        sources:
        - llvm-toolchain-trusty-4.0
    compiler: clang
    env: CC=clang-4.0 CXX=clang++-4.0 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - clang-5.0
        sources:
        - llvm-toolchain-trusty-5.0
    compiler: clang
    env: CC=clang-5.0 CXX=clang++-5.0 std=c++11
    os: linux
  - addons:
      apt:
        packages:
        - clang-5.0
        sources:
        - llvm-toolchain-trusty-5.0
    compiler: clang
    env: CC=clang-5.0 CXX=clang++-5.0 std=c++14
    os: linux
  - addons:
      apt:
        packages:
        - clang-5.0
        sources:
        - llvm-toolchain-trusty-5.0
    compiler: clang
    env: CC=clang-5.0 CXX=clang++-5.0 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - clang-5.0
        sources:
        - llvm-toolchain-trusty-5.0
    compiler: clang
    env: CC=clang-5.0 CXX=clang++-5.0 std=c++2a
    os: linux
script:
- mkdir build
- cd build
- STD=${std} cmake ..
- make
- ctest --output-on-failure
sudo: required
